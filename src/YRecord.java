public class YRecord {
    int month; // месяц
    int amount; // cумма
    boolean isExpense; // расходы

    public YRecord(int month, int amount, boolean isExpense) {
        this.month = month;
        this.amount = amount;
        this.isExpense = isExpense;
    }
}
